# facedetect_opencv
This is face detection using opencv and python.

It is tested in linux distribution and needs slight changes for others.

Run .python files as 'python photoface_det.py --cascade=/<path to cascade>/haarcascade_frontalface_default.xml'
